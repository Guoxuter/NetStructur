class Dataset(Dataset):
    # should give the dir where to get images and labels
    def __init__(self, image_dir, mask_dir, transform = None):
        pass
    def __len__(self):
        pass
    # should have a "index" to sent to the function
    def __getitem__(self, index):
            pass